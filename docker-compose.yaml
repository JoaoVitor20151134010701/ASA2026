services:
  nginx:
    build:
      context: .
      args:
        NGINX_VERSION: "1.25"
    container_name: meu-nginx
    ports:
      - "8080:80"
    environment:
      - NGINX_PORT=80
    volumes:
      - ./html:/usr/share/nginx/html
    restart: unless-stopped
